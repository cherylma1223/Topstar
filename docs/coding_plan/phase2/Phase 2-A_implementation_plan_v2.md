# [Goal Description]

在 Pass 1.5（技术动作分类）阶段引入多模态（视听融合）分析能力，并加入分层推理约束（CoT），以解决初学者动作变形导致的分类误判问题。

核心目标：
1. **听觉纠偏**：强制大模型倾听教练的语音口令（如"转腰"、"下蹲"），将语音作为判断用户"真实训练意图"的一级信号——但需根据语音清晰度和技术指向性分级使用。
2. **强制分层推理**：约束大模型必须先判断正反手方位（并评估方位置信度），再根据语音和动作幅度判断技术类型，杜绝"看反手位输出正手攻球"的幻觉错误。
3. **控制自由度**：将大模型采样温度（Temperature）维持在 0.2，在约束大模型分层推理的同时保留部分不确定性空间，以更好适应自动化场景下基于 confidence 字段的降级机制。

## 更新记录

| 日期 | 更新内容 |
|---|---|
| 2026-06-20 | 1. **并发锁升级**：在 `processAnalysisJob` 中增加原子级 SQL 更新锁（`AND status = 'queued'`），根治由开发环境或多进程导致的并发双重处理问题。<br>2. **前置缓冲扩展**：将 Pass 1 的视频片段裁剪前置缓冲从 1 秒增加至 3 秒，确保完全捕获击球前教练的关键口令。<br>3. **防底噪幻听与视听交叉验证**：在 Pass 1.5 修正了防混淆提示词的过度拟合问题。摒弃一刀切的硬编码屏蔽，要求大模型在遇到发音相近的短音节（如“对”和“蹲”）时，必须结合画面中“是否有明显的下蹲蓄力动作”进行交叉验证，避免将鼓励语误判为技术指令。 |

## User Review Required

> [!IMPORTANT]
> 引入语音判定后，大模型会更加聪明地猜测用户的**真实意图**。但这也意味着，如果视频环境极其嘈杂，或者背景里恰好有其他人在喊完全不相关的口令（比如旁边球台的教练在喊"拉球"），可能会对判断产生干扰。不过由于我们使用的是 Gemini 1.5 Pro 原生的多模态理解能力，它通常能较好地区分主体声音。



## Proposed Changes

### [MODIFY] [techniqueClassifier.ts](file:///Users/yingdongma/Documents/Dev/projects/Topstar/server/videoAnalysis/techniqueClassifier.ts)

**1. 引入【多模态与强制推理约束】提示词**

在 `knowledgePrompt` 中新增一段强约束规则，直接注入到系统级指令中。相比原版，本次更新做了以下改进：
- 语音判定增加**三级置信度分级**，避免模糊语音被过度采信
- 方位禁止规则增加**置信度门槛**，规避侧身正手等边界场景的误判
- `analysis_notes` 要求输出结构化的方位置信度与语音置信度，便于后续自动化断言

```typescript
  knowledgePrompt += `\n【多模态与强制推理约束】
你必须严格按照以下规则进行多模态分析与分层推理，禁止跳步：

第1步：聆听语音指令，并评估语音置信度
  仔细聆听视频中的语音/环境音，并按以下三级标准判断语音的可信程度：
  - 【高置信度】：语音清晰，且口令明确指向单一技术动作（如"拉球"、"摩擦"、"下蹲迎前"）。
    → 以语音意图为主，可覆盖视觉判断。
  - 【中置信度】：语音存在，但含义模糊或非技术性词汇（如"好"、"再来"、"注意"）。
    → 语音仅作参考，视觉判断为主。
  - 【低置信度/无语音】：无可辨识语音，或背景噪声无法区分主体声音。
    → 忽略语音，纯视觉判断。
  在 analysis_notes 中记录：你听到了什么、判定的语音置信度等级及理由。

第2步：判断击球方位，并评估方位置信度
  判断球员击球时的身体方位（正手位/反手位），同步评估置信度（高/中/低）：
  - 正手位：身体右侧（右手持拍者）
  - 反手位：身体左侧或身前偏左
  - 注意边界场景：侧身正手（球员在反手位用正手击球）应判定为"正手位，高置信度"，不可因位置在左侧而误判为反手位。
  方位禁止规则（仅在高置信度时生效）：
  - 高置信度反手位 → 禁止输出任何 fh_ 开头的 action_id
  - 高置信度正手位 → 禁止输出任何 bh_ 开头的 action_id
  - 中/低置信度方位 → 不强制限制，但须在 analysis_notes 中标注不确定性
  在 analysis_notes 中记录：判定的方位、方位置信度及理由。

第3步：综合研判技术类型
  结合以下信号进行综合判断：
  - 视觉信号：动作幅度（小撞击 vs 大幅度摩擦）、身体重心转移、随挥轨迹
  - 语音信号（依据第1步的置信度等级决定权重）
  重点注意：即使画面动作幅度较小（看起来像拨球），但如果第1步语音为【高置信度】且明确提示拉球技术要领，请以语音意图为准，将动作判定为拉球（如：bh_loop）。

第4步：在 analysis_notes 中输出结构化推理结论
  必须包含以下字段，便于系统自动校验：
  - 语音内容：[听到的具体内容，或"无可辨识语音"]
  - 语音置信度：[高/中/低]
  - 判定方位：[正手位/反手位/不确定]
  - 方位置信度：[高/中/低]
  - 最终 action_id：[选择的动作ID]
  - 选择理由：[综合视觉与语音的判断依据]
\n`;
```

**2. 维持 Temperature 在 0.2**

考虑到流水线当前为纯自动环境，不强行降至 0.0，保留模型输出不同候选方案不确定性的空间。

```typescript
      config: {
        temperature: 0.2, // 保持 0.2
        responseMimeType: 'application/json'
      }
```

> [!NOTE]
> 针对外部顾问建议中提到的修改 `confidence` 字段为字符串类型（'high' | 'medium' | 'low'），我们**不作修改**，保持现有根节点 `confidence: number` (0.0~1.0 浮点数) 的数据契约不变，将三级置信度评级全部放在 `analysis_notes` 字符串内作为辅助证据输出。

## Verification Plan

### Automated Tests

**1. 类型检查**
```bash
npx tsc --noEmit
```

**2. analysis_notes 结构化断言**

在自动化测试中加入对 `analysis_notes` 四步结构的断言，确保提示词约束被模型遵守：

```typescript
// 验证 analysis_notes 包含四步推理的关键结构字段
expect(notes).toMatch(/语音内容/);
expect(notes).toMatch(/语音置信度[：:]\s*(高|中|低)/);
expect(notes).toMatch(/判定方位[：:]\s*(正手位|反手位|不确定)/);
expect(notes).toMatch(/方位置信度[：:]\s*(高|中|低)/);
expect(notes).toMatch(/最终 action_id/);
expect(notes).toMatch(/选择理由/);
```

**3. 回归测试集（建议建立，可迭代扩充）**

| 测试场景 | 视频特征 | 预期输出 | 验证重点 |
|---|---|---|---|
| 核心场景：语音纠偏 | 动作幅度小（像拨球）+ 教练喊"拉球" | `bh_loop` | 语音覆盖视觉 |
| 边界场景：侧身正手 | 球员在反手位侧身用正手攻球 | `fh_*` 系列 | 方位置信度机制，不被位置误导 |
| 干扰场景：嘈杂背景音 | 多人说话，无法区分主体语音 | 视觉主导 | 低置信度语音不干扰判断 |
| 干扰场景：无关口令 | 旁边球台教练喊"拉球" | 视觉主导 | 语音来源区分 |
| 基准场景：无语音 | 安静环境，纯视觉 | 视觉主导 | 无语音时退化正常 |

### Manual Verification

提示词修改完成后，请再次上传那个"被认成反手拨球的拉球视频"（如果有教练画外音提示）。

观察后端控制台日志，重点检查：
1. `analysis_notes` 是否包含上述六个结构化字段
2. 语音置信度是否被正确评级（高/中/低）
3. 方位置信度是否影响了禁止规则的触发
4. `bh_drive` 是否因语音"高置信度拉球口令"被纠正为 `bh_loop`

---

## [2026-06-16 追加更新] Pass 1.5 音频转写 CoT 优化

**问题现象**：在极端嘈杂环境或存在大量视觉 Token（5FPS 长时间切片）干扰时，Gemini 容易“忽略”短促的口令词（如“蹲”、“起”），造成动作分类误判。

**解决方案**：
1. **重构 JSON Schema**：更新 `techniqueClassifier.ts` 的 `PASS15_SCHEMA`，在最前方强制插入 `audio_transcript` 字段。
2. **强制思维链 (CoT)**：约束模型必须先输出 `audio_transcript`（听写环境语音），再基于转写结果输出 `analysis_notes`（动作与语音综合推理）。
3. **针对性警告词**：在提示词第一步“聆听语音指令”下，追加对“短促单音节口令（如蹲、起、拉）在乒乓球馆环境极易被掩盖”的高优先级警告。
